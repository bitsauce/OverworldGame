#ifndef GAME_SCENE_H
#define GAME_SCENE_H

/*#include "Config.h"
#include "Scene.h"

class GameOverlay;
class World;

class GameScene : public Scene
{
public:
	GameScene(Scene *scene, World *world);
	~GameScene();

public:
	GameOverlay *m_inventory;
	World *m_world;
};*/

#endif // GAME_SCENE_H